[project]
name = "standards-fabric"
version = "0.1.0"
description = "Facility standards twin (past / now / future, per zone) + multi-agent standards & EU watch for the Swedish IoT consortium. Stdlib only."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "WINNIIO AB", email = "ceo@winniio.io"}]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.5"]

[project.scripts]
standards-fabric = "standards_fabric.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 130
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B"]
ignore = ["E501"]
